// Navigation config — populate as pages are built

export const pageLinks: { subheader: string; items: { title: string; path: string }[] }[] = [];

export const navConfig: { title: string; path: string }[] = [];
